# Phase 4 Code Review — Follow-ups

> Captured from Phase 4 implementation. Phase 4 was approved to ship with these items as non-blocking follow-ups. Address in a post-v1 cycle or when the relevant harness lands.

## Deferred

### Playwright smoke for quote generation (Task 4.7-B)

The Phase 4 plan included a Playwright end-to-end smoke test covering the quote generation flow (login → scenario creation → Generate Quote button → history page). Skipped because no Playwright harness is installed in the repo.

When Playwright is introduced (likely alongside the first e2e test in any phase), add a quote-generation smoke as described in the Phase 4 spec, section "Task 4.7-B".
